[ -e /var/service/dbus ] && sv -w 15 wait dbus >/dev/null 2>&1

if [ -x /sbin/agetty -o -x /bin/agetty ]; then
	if [ "${tty}" = "tty1" ]; then
		GETTY_ARGS="--autologin @USERNAME@ --noissue --noclear"
	fi
fi

BAUD_RATE=38400
TERM_NAME=linux
